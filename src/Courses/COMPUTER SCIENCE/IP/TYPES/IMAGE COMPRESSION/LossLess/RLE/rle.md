🔹 Run-Length Encoding (RLE)

RLE is a lossless compression technique that replaces consecutive repeated values with a single value and a count.

Instead of storing:

the same thing over and over again

It stores:

what it is + how many times it appears