🔷 2. DBMS (Database Management System)

A DBMS is software that manages databases.

Lets you store, retrieve, update data
Controls access and security
Handles backup, recovery

👉 Examples:

MongoDB
MySQL

📌 Database = data
📌 DBMS = tool to manage that data

Without DBMS, you’re manually digging through files like it’s 1995.