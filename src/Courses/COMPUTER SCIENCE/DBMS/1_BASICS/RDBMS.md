🔷 3. RDBMS (Relational DBMS)

An RDBMS is a type of DBMS that stores data in tables with relationships.

Uses rows and columns
Tables are connected using keys
Uses SQL

👉 Examples:

MySQL
PostgreSQL

📌 RDBMS = smarter DBMS with structure + relationships